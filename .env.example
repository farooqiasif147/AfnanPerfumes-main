# Public (client-side) env vars for Vite builds.
# Copy to `.env` for local dev, and set the same variable in Cloudflare Pages for production.

# Google Apps Script Web App URL that writes orders to Google Sheets
VITE_GOOGLE_SHEETS_URL="https://script.google.com/macros/s/REPLACE_WITH_YOUR_ID/exec"
